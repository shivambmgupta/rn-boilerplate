import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    googleButton: {
        padding: 10,
        width: 100,
        height: 100,
        borderRadius: 50,
        justifyContent: "center",
        alignItems: "center"
    },
    googleText: { fontSize: 25, fontWeight: "bold" }
})